# ML 튜터 작업지시서
## 데이터 기반 기계학습 모델 구현을 위한 AI 튜터 & 멘토형 서비스
> v0.1 MVP 개발 지시서 | 2025

---

## 1. 프로젝트 개요

본 서비스는 고등학교 정보 교과에서 데이터 기반 기계학습 모델 구현을 학습하는 학생들을 위한 AI 튜터 및 멘토형 웹 서비스입니다.

### 핵심 철학
- 정답 코드를 직접 제공하지 않음 — 인지적 비계(Scaffolding) 방식으로 학생이 스스로 해결하도록 유도
- 기존 AI 챗봇과의 차별점: 현재 수업 단계와 미션 맥락을 인식한 구조화된 AI 튜터
- 과정 중심 평가: 결과물뿐 아니라 학습 여정 전체를 기록 및 평가

### 사용 환경
- 대상: 고등학교 정보 교과 학생 (최대 300명)
- 기기: 교실 PC + 학생 스마트폰 모두 지원
- 주의: 학교 PC는 재부팅 시 브라우저 데이터 초기화 → 반드시 서버 저장 필요

---

## 2. 개발 마일스톤

### MVP (v0.1) — 핵심 틀 구현
> 목표: 전체 화면 구조와 AI 튜터 연동이 작동하는 프로토타입

- [ ] 3패널 레이아웃 구현 (진행패널 + 가이드패널 + AI튜터 채팅)
- [ ] 7단계 진행 패널 (잠금/해제 구조)
- [ ] 학습 → 퀴즈 → 실습 미션 탭 전환 흐름
- [ ] Claude API 연동 AI 튜터 채팅 (단계+미션 맥락 주입)
- [ ] 3단계(데이터 전처리) 샘플 콘텐츠로 전체 동작 확인
- [ ] 시작 화면: 학생 이름/학번 입력 + 교사 코드 입력

### v0.2 — 저장 및 여정 기록
> 목표: Firebase 연동으로 데이터 영구 저장

- [ ] Firebase Authentication (구글 계정 로그인)
- [ ] Firebase Firestore — 진행 현황, 퀴즈 결과, AI 대화 내역 저장
- [ ] 이미지 저장: 학생 구글 드라이브 연동 (Firestore에는 URL 링크만 저장)
- [ ] 미션 완료 팝업 — 텍스트 기록 + 이미지 업로드 (최대 5장/단계, 200KB 자동 압축)
- [ ] [내 여정] 탭 — 타임라인 화면 구현
- [ ] PDF 출력 — 타임라인 + 이미지 포함 보고서

### v0.3 — 갤러리 및 상호평가
> 목표: 7단계 완료 후 학생 간 공유 및 평가 기능

- [ ] 7단계 완료 시 갤러리 화면으로 자동 전환
- [ ] 갤러리 페이지 — 학생 결과물 카드 형태 표시
- [ ] 항목별 평가 (★ 5점 척도 5개 항목 + 한줄 피드백)
- [ ] 평가자 익명 처리 (학생 간), 교사는 평가자 확인 가능

### v0.4 — 교사 대시보드
> 목표: 교사가 학생 현황을 실시간으로 관리

- [ ] 교사 코드 입력 시 교사 전용 화면으로 전환
- [ ] 학생별 진행 단계/미션 현황 대시보드
- [ ] 학생 타임라인 열람 + 이미지 확인
- [ ] 상호평가 평가자 확인 (교사만)
- [ ] PDF 일괄 출력 기능

### v1.0.0 — 전체 콘텐츠 완성
> 목표: 1~7단계 전체 콘텐츠 탑재 및 완성도 높은 서비스

- [ ] 1~7단계 전체 학습 콘텐츠 (개념 설명, 문법 예시, 퀴즈, 미션, 코드 템플릿)
- [ ] 반응형 레이아웃 완성 (교실 PC 1280px + 스마트폰 360px)
- [ ] 최종 QA 및 안정화

---

## 3. MVP (v0.1) 상세 개발 지시

### 3-1. 기술 스택

| 영역 | 기술 | 비고 |
|------|------|------|
| 프론트엔드 | React + Tailwind CSS | 단일 페이지 앱 |
| AI 엔진 | Claude API (`claude-sonnet-4-20250514`) | 단계+미션 맥락 시스템 프롬프트 주입 |
| 코드 표시 | 신택스 하이라이팅 | Prism.js 또는 직접 구현 |
| 저장 (MVP) | React 상태 (useState) | MVP는 메모리 내 저장 |
| 저장 (v0.2+) | Firebase Firestore + Storage | 구글 계정 로그인 연동 |

### 3-2. 화면 구성

#### 시작 화면
- 학생 모드: 이름 + 학번 입력 → 고유 코드 발급 후 메인 화면 진입
- 교사 모드: 교사 코드 입력 → 교사 전용 대시보드 화면 전환

#### 메인 화면 — 3패널 구조
```
┌─────────────┬──────────────────────────┬─────────────────┐
│ 📋 진행패널  │ 📖 가이드 패널             │ 🤖 AI 튜터 채팅  │
│  (200px)    │ [학습][퀴즈][실습][내여정]  │   (320px)       │
│             │                          │                 │
│ 1✅ 개념    │ 현재 탭 내용 표시          │ 현재 단계+미션   │
│ 2✅ 아이디어 │                          │ 맥락 인식 응답   │
│ 3🔵 전처리  │                          │                 │
│  🔵 미션1   │                          │ 정답 직접 제공 X │
│  🔒 미션2   │                          │ 힌트·질문으로 유도│
│ 4🔒 모델링  │                          │                 │
│ 5🔒 학습    │                          │                 │
│ 6🔒 시각화  │                          │                 │
│ 7🔒 평가    │                          │                 │
└─────────────┴──────────────────────────┴─────────────────┘
```

#### 단계별 내부 흐름
```
① 학습 탭
   └── 개념 설명 + 문법 예시 + 코드 템플릿 카드
   └── AI 튜터: 학습 내용 관련 보충 질문 응답

        ↓ 퀴즈 통과해야 실습 잠금 해제 🔒

② 퀴즈 탭
   └── 객관식 3문제
   └── 모두 통과 시 실습 탭 잠금 해제

③ 실습 탭 (미션 수행)
   └── 미션 목록 순차 표시 (완료해야 다음 미션 해제)
   └── 미션별 코드 템플릿 카드 제공 (변수명만 수정)
   └── AI 튜터: 현재 미션 기준으로만 힌트 제공

④ 내 여정 탭
   └── 타임라인 형태로 학습 기록 표시
   └── PDF 출력 버튼
```

#### 7단계 완료 후 — 갤러리로 전환
- 메인 화면 전체가 갤러리 화면으로 전환
- 학생 결과물 카드 형태 표시
- 항목별 평가 및 한줄 피드백 작성 가능

#### 교사 화면
- 시작 화면에서 교사 코드 입력 시 교사 전용 대시보드로 전환
- 탭 구성: [학생현황] [여정열람] [상호평가관리]

---

### 3-3. AI 튜터 동작 규칙

#### 시스템 프롬프트 필수 포함 항목
```
- 현재 단계 번호 및 단계명
- 현재 미션 번호 및 미션명
- 현재 탭 (학습 / 실습)
- 스캐폴딩 원칙 (정답 직접 제공 금지)
```

#### 시스템 프롬프트 템플릿
```
당신은 고등학교 정보 교과 기계학습 수업의 AI 튜터입니다.

현재 학생 상태:
- 단계: {stepNumber}단계 - {stepName}
- 현재 탭: {currentTab} (학습 / 실습)
- 현재 미션: {missionNumber}번 - {missionName}

응답 원칙:
1. 절대로 완성된 정답 코드를 직접 제공하지 않는다.
2. 힌트와 유도 질문으로만 응답한다.
3. 현재 단계/미션 범위를 벗어난 질문은 현재 단계로 집중 유도한다.
4. 학습 탭에서는 해당 단계 학습 내용 관련 질문에만 응답한다.
5. 학생이 스스로 생각하고 코드를 수정하도록 격려한다.
```

---

### 3-4. 3단계 샘플 콘텐츠 (데이터 전처리)

#### 학습 모드 콘텐츠 (3개 개념)

**개념 1: 결측치(NaN)**
- 설명: 데이터에서 값이 비어있는 경우
- 문법: `isnull()`, `dropna()`, `fillna()`
- 예시 코드:
```python
# 결측치 확인
df.isnull().sum()

# 결측치가 있는 행 제거
df = df.dropna()

# 결측치를 평균값으로 채우기
df = df.fillna(df.mean())
```

**개념 2: 이상치**
- 설명: 데이터의 전반적인 패턴에서 크게 벗어난 값
- 문법: `describe()`, `boxplot()`
- 예시 코드:
```python
# 기술통계로 이상치 탐색
df.describe()

# boxplot으로 시각화
import matplotlib.pyplot as plt
df.boxplot()
plt.show()
```

**개념 3: 데이터 타입 변환**
- 설명: 문자열로 저장된 숫자 등을 올바른 타입으로 변환
- 문법: `dtypes`, `astype()`, `LabelEncoder()`
- 예시 코드:
```python
# 데이터 타입 확인
df.dtypes

# 숫자형으로 변환
df['컬럼명'] = df['컬럼명'].astype(float)

# 문자형 → 숫자형 (레이블 인코딩)
from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()
df['컬럼명'] = le.fit_transform(df['컬럼명'])
```

#### 퀴즈 콘텐츠 (3문제)

```js
[
  {
    id: 1,
    question: "결측치를 확인하는 함수는 무엇인가요?",
    options: ["isnull()", "dropna()", "fillna()", "describe()"],
    answer: 0
  },
  {
    id: 2,
    question: "결측치가 있는 행을 제거하는 함수는 무엇인가요?",
    options: ["isnull()", "dropna()", "fillna()", "astype()"],
    answer: 1
  },
  {
    id: 3,
    question: "결측치를 특정 값으로 채우는 함수는 무엇인가요?",
    options: ["isnull()", "dropna()", "fillna()", "describe()"],
    answer: 2
  }
]
```

#### 실습 미션 목록 (5개)

```js
[
  {
    id: 1,
    title: "CSV 파일 불러오기",
    description: "분석할 CSV 파일을 pandas로 불러오세요.",
    hint: "pd.read_csv() 함수를 사용해보세요. 괄호 안에는 무엇이 들어갈까요?",
    template: `import pandas as pd\n\ndf = pd.read_csv('_____.csv')\ndf.head()`
  },
  {
    id: 2,
    title: "데이터 구조 확인",
    description: "불러온 데이터의 크기와 구조를 확인하세요.",
    hint: "df.shape, df.info(), df.head() 를 각각 실행해보세요. 어떤 정보를 알 수 있나요?",
    template: `# 데이터 크기 확인\nprint(df.shape)\n\n# 컬럼 정보 확인\ndf.info()\n\n# 상위 5행 확인\ndf.head()`
  },
  {
    id: 3,
    title: "결측치 확인 및 처리",
    description: "데이터에 결측치가 있는지 확인하고 처리하세요.",
    hint: "먼저 isnull().sum()으로 결측치 개수를 확인해보세요. 몇 개나 있나요?",
    template: `# 결측치 확인\nprint(df.isnull().sum())\n\n# 결측치 처리 (행 제거 또는 채우기)\ndf = df._____`
  },
  {
    id: 4,
    title: "이상치 탐색",
    description: "데이터에 이상치가 있는지 확인하세요.",
    hint: "df.describe()로 기술통계를 확인해보세요. min, max 값이 이상하지 않나요?",
    template: `# 기술통계로 이상치 탐색\ndf.describe()\n\n# boxplot으로 시각화\nimport matplotlib.pyplot as plt\ndf.boxplot()\nplt.show()`
  },
  {
    id: 5,
    title: "데이터 타입 확인 및 변환",
    description: "각 컬럼의 데이터 타입을 확인하고 필요시 변환하세요.",
    hint: "df.dtypes로 각 컬럼의 타입을 확인해보세요. 숫자여야 하는데 object 타입인 컬럼이 있나요?",
    template: `# 데이터 타입 확인\nprint(df.dtypes)\n\n# 타입 변환이 필요한 컬럼 변환\ndf['컬럼명'] = df['컬럼명'].astype(_____)`
  }
]
```

---

### 3-5. 콘텐츠 데이터 구조 (표준 형식)

모든 단계 콘텐츠는 아래 구조를 따릅니다. `src/data/` 폴더에 단계별 파일로 분리합니다.

```js
// src/data/step3_content.js 예시
export const step3Content = {
  stepId: 3,
  stepName: "데이터 전처리",
  learn: [
    {
      id: 1,
      title: "결측치(NaN)란?",
      description: "데이터에서 값이 비어있는 경우를 결측치라고 합니다.",
      code: "df.isnull().sum()"
    }
  ],
  quiz: [
    {
      id: 1,
      question: "결측치를 확인하는 함수는?",
      options: ["isnull()", "dropna()", "fillna()", "describe()"],
      answer: 0
    }
  ],
  missions: [
    {
      id: 1,
      title: "CSV 파일 불러오기",
      description: "분석할 CSV 파일을 pandas로 불러오세요.",
      hint: "pd.read_csv() 함수를 사용해보세요.",
      template: "import pandas as pd\n\ndf = pd.read_csv('_____.csv')\ndf.head()"
    }
  ]
};
```

---

## 4. 학생 여정 기록 (타임라인) 설계

### 저장 구조

| 데이터 종류 | 저장 방식 | 저장 위치 |
|-----------|---------|---------|
| 진행 현황, 퀴즈, AI 대화 | 자동 저장 | Firebase Firestore |
| 학생 텍스트 기록 | 미션 완료 팝업에서 입력 | Firebase Firestore |
| 이미지 캡처 | 파일 업로드 (최대 5장/단계, 200KB 압축) | 학생 구글 드라이브 |
| 이미지 링크 | 자동 저장 | Firebase Firestore |

### 미션 완료 팝업 구성
```
┌─────────────────────────────────┐
│ 🎯 미션 완료!                    │
│                                 │
│ ✏️ 오늘 배운 점을 기록해보세요    │
│ [ 텍스트 입력창                 ] │
│                                 │
│ 🖼️ 결과 화면 캡처 첨부 (선택)    │
│ [ 📎 이미지 업로드 (최대 5장)   ] │
│ [ 썸네일 미리보기               ] │
│                                 │
│ [건너뛰기]      [저장하기 ✅]    │
└─────────────────────────────────┘
```

### 타임라인 화면 구성
```
[나의 ML 프로젝트 여정] — 홍길동
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 3단계 | 데이터 전처리  2025.05.12
│
├── 🤖 자동 기록
│   └── 미션1 ✅ 완료 (14:23)
│   └── AI 튜터 대화 3회
│   └── 퀴즈 3/3 정답
│
├── ✏️ 학생 기록
│   └── "결측치 23개 → dropna 처리"
│
└── 🖼️ 캡처 이미지 썸네일 (클릭 시 원본)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[📄 PDF 출력]  [💬 전체 AI 대화 보기]
```

---

## 5. 상호평가 설계

### 갤러리 화면 (7단계 완료 후 메인 화면 전환)
```
┌──────────────┬──────────────┬──────────────┐
│ 👤 홍길동     │ 👤 김철수     │ 👤 이영희     │
│ 저출산 예측   │ 미세먼지 분류  │ 기온 예측     │
│ R²: 0.91    │ 정확도: 87%  │ R²: 0.79    │
│ [자세히 보기] │ [자세히 보기] │ [자세히 보기] │
└──────────────┴──────────────┴──────────────┘
```

### 항목별 평가 (5개 항목)
1. 주제의 사회적 의미 ★★★★☆
2. 데이터 전처리 과정 ★★★★★
3. 모델 선택의 적절성 ★★★☆☆
4. 시각화 표현력 ★★★★☆
5. 설명의 명확성 ★★★★☆

### 평가자 공개 설정
- 학생 간: 익명 처리
- 교사: 누가 평가했는지 확인 가능

---

## 6. 교사 대시보드 설계

### 접근 방법
- 시작 화면에서 교사 코드 입력 시 교사 전용 화면으로 전환

### 탭 구성

#### [학생현황] 탭
| 이름 | 단계 | 미션 | AI 대화 횟수 | 마지막 접속 |
|------|------|------|------------|----------|
| 홍길동 | 3단계 | 미션2/5 | 5회 | 2025.05.12 14:23 |
| 김철수 | 4단계 | 미션1/3 | 3회 | 2025.05.13 10:15 |

#### [여정열람] 탭
- 학생 선택 → 타임라인 전체 열람
- 업로드된 이미지 확인 (구글 드라이브 링크)

#### [상호평가관리] 탭
- 평가자 확인 (학생에게는 익명)
- 갤러리 공개 시점 설정

---

## 7. 개발 주의사항

### Claude API 호출
- 모델: `claude-sonnet-4-20250514` 고정
- `max_tokens`: 1000 고정
- API 키는 반드시 환경변수(`.env`)에서 로드, 하드코딩 절대 금지

### 저장 관련
- MVP: React `useState`만 사용
- `localStorage` / `sessionStorage` 사용 금지 (학교 PC 재부팅 시 초기화)
- v0.2+: Firebase Firestore 사용
- 이미지: 학생 구글 드라이브 저장, Firestore에는 URL만 저장
- 이미지 자동 압축: 200KB 이하
- 단계당 이미지 최대 5장 제한

### UI/UX
- `position: fixed` 사용 금지
- 모든 색상은 CSS 변수 사용 (다크모드 대응)
- 반응형: 교실 PC (1280px 이상) + 스마트폰 (360px 이상)
- 퀴즈 통과 전까지 실습 탭 잠금 유지
- 미션은 순차적으로만 해제

---

## 8. 프로젝트 폴더 구조

```
ml-tutor/
├── CLAUDE.md                    # Claude Code 지시 파일
├── .env                         # 환경변수 (API 키 등)
├── package.json
├── public/
│   └── index.html
└── src/
    ├── App.jsx                  # 메인 앱
    ├── components/
    │   ├── StartScreen.jsx      # 시작 화면
    │   ├── MainLayout.jsx       # 3패널 레이아웃
    │   ├── ProgressPanel.jsx    # 좌측 진행 패널
    │   ├── GuidePanel.jsx       # 중앙 가이드 패널
    │   ├── TutorChat.jsx        # 우측 AI 튜터
    │   ├── tabs/
    │   │   ├── LearnTab.jsx     # 학습 탭
    │   │   ├── QuizTab.jsx      # 퀴즈 탭
    │   │   ├── MissionTab.jsx   # 실습 미션 탭
    │   │   └── JourneyTab.jsx   # 내 여정 탭
    │   └── teacher/
    │       └── TeacherDashboard.jsx
    ├── data/
    │   ├── step3_content.js     # 3단계 샘플 콘텐츠
    │   └── steps_index.js       # 전체 단계 목록
    └── utils/
        └── claudeApi.js         # Claude API 호출 유틸
```

---

## 9. 다음 단계 안내

MVP 완성 후 아래 순서로 진행합니다.

1. MVP (v0.1) 동작 확인 및 피드백
2. 1~7단계 전체 콘텐츠 선생님과 함께 작성
3. v0.2 Firebase 저장 연동
4. v0.3 갤러리 및 상호평가
5. v0.4 교사 대시보드
6. v1.0.0 전체 콘텐츠 탑재 및 완성
